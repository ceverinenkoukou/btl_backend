# Copier ce fichier en .env et remplir les valeurs réelles
# NE JAMAIS committer le fichier .env dans git

SECRET_KEY=changez-moi-en-production

DEBUG=True

ALLOWED_HOSTS=localhost,127.0.0.1,btlbackend-production.up.railway.app,*.railway.app


# Base de données PostgreSQL (noms alignés avec l'image Docker postgres + settings.py)
# IMPORTANT : utiliser uniquement des caractères ASCII dans le mot de passe (pas d'accents)
# Base de données PostgreSQL
POSTGRES_DB=btl_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=btl_password_ascii
POSTGRES_HOST=btl-db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:yMrasFZLfQblMHEWMWsfkQOdbiFldWSQ@postgres.railway.internal:5432/railway
BACKEND_PORT=8003

# Email
# Sans EMAIL_HOST_USER : les e-mails s'affichent dans la console Django
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend


# Email (Désormais géré via API Gmail en production)
DEFAULT_FROM_EMAIL=MHédia BTL <contact@mhedia-ga.com>

# Frontend URL (pour les liens dans les emails)
FRONTEND_URL=https://btl-frontend.vercel.app

# Frontend URL (pour les liens dans les emails)
FRONTEND_URL=https://btl-frontend.vercel.app



# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://btl-frontend.vercel.app

#CELERY
CELERY_BROKER_URL=redis://btl-redis:6379/0